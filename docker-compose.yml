services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - DEV=True
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
    command: >
      sh -c "pip install -r requirements.txt && python manage.py runserver 0.0.0.0:8000"

